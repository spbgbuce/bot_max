"""
    Модуль bot.py

    Данный модуль является точкой входа (entry point) для запуска системы мониторинга.
    Он инициализирует центральный компонент Processor, передавая ему конфигурацию,
    и запускает процесс мониторинга. Вся дальнейшая работа (воркеры, постер, БД)
    управляется процессором.

    Содержимое:
    -----------
    - Импорт Processor из модуля processor и конфигурации из config.
    - Создание экземпляра Processor с параметрами config.
    - Запуск процессора через метод start().

    Ключевые зависимости:
    ----------------------
    - processor.Processor: Центральный класс, управляющий воркерами и постером.
    - config: Модуль, содержащий словарь конфигурации (настройки HTTP-сайтов,
      Proxmox, интервалы проверок и т.д.).

    Пример запуска:
    ---------------
    # В командной строке:
    python bot.py

    Примечания:
    -----------
    - Программа работает бесконечно до принудительного прерывания (Ctrl+C).
    - Логирование настраивается в модуле config или processor, в данном файле
      его настройка отсутствует.
"""

from processor import Processor
from config import config


processor = Processor(config)
processor.start()